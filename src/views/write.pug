extends base.pug

block content 
  h1 Write how was your today?
  form(method="POST")
    input(type="text", name="title", placeholder="제목을 작성하세요")
    textarea(name="memo", cols="30", rows="10", placeholder="현재 마음을 적어보세요...", minLength="20")
    input(type="submit", value="Submit")