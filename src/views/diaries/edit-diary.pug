extends ../config/base.pug

block content 
   form(method="POST")
      p.title 다이어리 수정하기
      label(for="title") 제목
      input(type="text", name="title", value=diary.title)
      label(for="memo") 제목
      textarea(name="memo", id="memo", cols="30", rows="10", minLength="20")=diary.memo
      input(type="submit", value="저장하기")