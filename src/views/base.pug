doctype html 
html(lang="ko")
  head
    title #{pageTitle} | #{siteName}
    link(rel="stylesheet" href="https://unpkg.com/mvp.css")
  body 
    header 
      h1=pageTitle
      ul 
        li
          a(href="/") 홈 
        li
          a(href="/diary/write") 다이어리 작성하기
        if loggedIn 
          li 
            a(href="/logout") 로그아웃
          li
            a(href="/users/edit") 프로필 수정
        else
          li
            a(href="/join") 회원가입
          li
            a(href="/login") 로그인 
    main 
      block content
    include partials/footer.pug